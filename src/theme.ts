import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    colors: {
        primary: "#D3AE6D",
        secondary: "#373435",
        navbar: "#CECECD",
        brand: {
            500: "#0077ff",
        },
    },
});


export default theme;
